
  # Dating app with swiping feature

  This is a code bundle for Dating app with swiping feature. The original project is available at https://www.figma.com/design/TifIPCPEj6x86DTJorG5h3/Dating-app-with-swiping-feature.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  